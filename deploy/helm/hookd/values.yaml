image:
  repo: ""
  tag: ""
  pullPolicy: Always


config:
  minReplicas: "1"
  maxReplicas: "5"

service:
  type: LoadBalancer
  externalPort: 8888
  ip: ""

resources: {}

deployment:
  nodePool: api
